Cinematic hero image for an open-source project, 1200x630, deep dark background (#0a0a0f).

Two glowing icons centered in the frame, connected by a stream of luminous particles.

Left icon: a database/stacked-layers icon rendered in cyan (#00d4ff), emitting a soft glow with fine particle details circling inside. Represents structured data storage.

Right icon: a brain/spark/neural-network icon rendered in magenta (#c084fc), emitting a soft glow with organic pulse ripples expanding outward. Represents an AI agent.

Between them: a stream of small cyan particles flows left-to-right in orderly formation, then destabilizes at the midpoint where it transforms into magenta organic pulse waves — an energetic conversion zone where both colors blend and glow brightest.

Text (small, secondary to the icons):
- Top center: "Personal Data Warehouse × AI Agent Runtime" in white sans-serif
- Below left icon: "EgoGraph" in white bold sans-serif
- Below right icon: "EgoPulse" in white bold sans-serif

Style: dark cinematic premium tech aesthetic. Clean flat icons with subtle 3D depth and bloom/glow — think Figma-quality icon design rendered on a dark cinematic canvas. Vast negative space. Only the icons, particles, and text exist — nothing else. Color palette: cyan #00d4ff, magenta #c084fc, white text, on #0a0a0f black.
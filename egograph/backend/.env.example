# ==============================================
# EgoGraph Backend Configuration
# ==============================================

# サーバー設定
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
BACKEND_RELOAD=true
# BACKEND_API_KEY=your-secure-backend-api-key  # 未設定時は認証なし（開発用）

# CORS設定 (カンマ区切り)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ロギング設定 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ==============================================
# LLM (Language Model) Configuration
# ==============================================
# 各プロバイダーのAPIキー（使用するプロバイダーのみ設定）
OPENAI_API_KEY=sk-your-openai-api-key
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key
OPENROUTER_API_KEY=sk-or-your-openrouter-api-key

# デフォルトモデル（未指定時のフォールバック）
DEFAULT_LLM_MODEL=xiaomi/mimo-v2-flash:free

# 生成パラメータ
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2048

# OpenRouter固有の設定
# Web検索を有効にするか（デフォルト: false）
# trueにすると追加料金が発生する可能性があります
LLM_ENABLE_WEB_SEARCH=false

# ==============================================
# Cloudflare R2 Configuration (必須)
# Data Lakeへのアクセスに必要です
# ==============================================

# R2バケットのエンドポイントURL
R2_ENDPOINT_URL=https://your-account-id.r2.cloudflarestorage.com

# R2 アクセスキー
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key

# バケット名
R2_BUCKET_NAME=egograph

# イベントデータのパスプレフィックス (末尾のスラッシュは必須かも実装依存だが基本つける)
R2_EVENTS_PATH=events/

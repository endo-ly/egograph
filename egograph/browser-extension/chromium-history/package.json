{
  "name": "chromium-history-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && mkdir -p dist/options && cp manifest.json dist/manifest.json && cp src/options/index.html dist/options/index.html",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
